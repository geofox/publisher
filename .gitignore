# built binaries (produced by `go build`)
/publisher
/cmd/publisher/publisher
/nostr-publisher

# brainstorming mockups (visual companion)
.superpowers/

# local design specs — kept out of the public repo (live local / private repo)
docs/superpowers/
